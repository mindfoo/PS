Classes to represent the data stored in the database. 
These classes are used by the repository to interact 
with the database and perform CRUD operations. 
They are also used by the service layer to 
manipulate the data before sending it to the client.